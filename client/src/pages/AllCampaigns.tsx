import { CampaignsList } from "@/components/CampaignsList";

export default function AllCampaignsPage() {
  return <CampaignsList title="All Campaigns" description="Every campaign you've ever created, not just the 5 most recent" />;
}
